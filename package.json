{
  "private": true,
  "dependencies": {
    "dotenv": "^6.2.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "jest": "^23.6.0",
    "lerna": "^3.4.3",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.10.5",
    "tslint": "^5.11.0",
    "tslint-sonarts": "^1.8.0",
    "typescript": "^3.2.0"
  },
  "scripts": {
    "build": "cd packages; tsc -b && lerna run build",
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "serve": "lerna run serve",
    "test": "lerna run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
