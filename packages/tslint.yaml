extends:
  - "tslint:recommended"
  - tslint-react
  - tslint-sonarts
rules:
  interface-name:
    options: [never-prefix]
  typedef:
    options: [call-signature]
  object-literal-sort-keys: false
  jsx-boolean-value:
    options: [never]
